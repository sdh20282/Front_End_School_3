#####################
css 실무

디자인적인 개성을 주기 위해 실무에서는 input 요소나 select 박스를 브라우저 스타일과 다르게 표현하곤 함
- 브라우저마다 스타일이 모두 다름
-- 맞추기 어려움
-- 스타일이 까다로움

포커싱 필수

아웃라인
- 레이아웃에 영향을 미치지 않음
- 보더
-- 영향을 미침
-- 박스 안에 들어감

셀렉트
- 모바일은 커스텀 하지 않음
- 피씨만

인풋은 요소를 숨겨서 사용
셀렉트는 새로 만듦ㄴ

자바스크립스 스타일은 인라인으로 적용됨
- 협업할 떄 문제 발생
- 스타일과 제어를 확실히 분리

IR 테크닉
- 디자인적으로 보이지는 않지만 스크린 리더나 브라우저를 위해 정보를 전달하는 텍스트를 html곳곳에 숨겨두는 방법

백그라운드 이미지 안나올때도

이미지 폰트

네이버 마진 -1픽셀
- IE 구버전에서 인식을 못하는 경우가 있기 때문에 margin: -1px; 을 사용합니다.

클립은 앱솔루트에서만
- 더이상 사용하지는 않음
- clip path
-- 인셋
- 요소가 차지하던 공간 자체는 남아있음
-- 렌더링할때 공간을 잘라버림

각각의 수치만큼 각각의 요소의 위치를 자른다

aria-label
- 대체 텍스트 최신 버전

이미지 스프라이트
- 이미지 1번만 로딩하면 됨
- 수정이 어려움
-- 능동적이지 못함
- 이미지가 계속 추가되면 스프라이트 자체의 이미지가 너무 커질 수도 있음
-- 쪼개서 사용
-- 로딩이 느려질 수 잇음
- 관리가 어려움
-- 이미지 하나 변경시 전체 변경해야 함
-- 이미지가 어디서 사용되는지 알기 힘들다

base64image
- 이미지를 코드로 바꿔줌
-- 이미지를 브라우저에서 그려줌
-- 용량이 낮아짐

레티나
- 특정한 시야 거리에서 인간의 눈으로는 화소를 구분할 수 없는 화소 밀도(300 PPI가 넘을 경우)를 가진 애플 LCD 제품의 브랜드 이름
- 일단 기기에 비해 화소가 2배로 높음
-- 같은 해상도의 이미지가 줄어듦
-- 하지만 픽셀값을 명령했기 때문에 채워야 함
-- 화질이 안좋아지게 됨
2배 화소의 이미지 준비

















